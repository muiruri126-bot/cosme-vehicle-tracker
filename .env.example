# COSME Vehicle Tracker – Environment Variables
# Copy this file to .env and fill in the values.

# Flask secret key (change this in production!)
SECRET_KEY=change-me-to-a-random-string

# Database URL (SQLite by default, use PostgreSQL in production)
# DATABASE_URL=sqlite:///tracker.db
# DATABASE_URL=postgresql://user:password@localhost:5432/cosme_tracker

# Mail settings (set MAIL_ENABLED=1 to activate email notifications)
MAIL_ENABLED=0
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_DEFAULT_SENDER=noreply@cosme-project.org

# Flask debug mode (set to 0 in production)
FLASK_DEBUG=1
